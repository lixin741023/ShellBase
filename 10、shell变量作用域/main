// Shell 变量的作用域可以分为三种：
// |>
// 有的变量只能在 函数内部 使用，这叫做局部变量（local variable）；
// 有的变量可以在当前 Shell进程 中使用，这叫做全局变量（global variable）；
// 而有的变量还可以在 子进程 中使用，这叫做环境变量（environment variable）。


// 在不同的作用域中，同名的变量不会相互干涉。
