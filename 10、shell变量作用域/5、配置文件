// 有读者可能会问，如果我想让一个变量在所有 Shell 进程中都有效，不管它们之间是否存在父子关系，该怎么办呢？

// 只有将变量写入 Shell 配置文件中才能达到这个目的！！（唯一的办法）
// <|
// 因为：
// Shell 进程每次启动时都会执行配置文件中的代码做一些初始化工作，
// 如果将变量放在配置文件中，那么每次启动进程都会定义这个变量。（进程中都会包含这个变量）

// 有关具体的内容可以参考：5、自己做1个命令
