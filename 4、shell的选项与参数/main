// 在键入 shell命令 的时候，我们可以带有 选项 或 参数。


// Linux 的选项又分为 短格式选项 和 长格式选项。

// 短格式选项是长格式选项的简写，用一个 - 和 一个字母 表示，例如 ls -l。
// 长格式选项是完整的英文单词，用 -- 和 一个单词表示，例如 ls --all。

// 所以
// ls -l 和 ls --all 的作用是一样的，
// <|
// 基本上1个 短格式选项 就对应1个 长格式选项，
// 但这不是绝对的哦～
