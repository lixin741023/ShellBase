// 参数


// 所以命令后面的 选项、参数，他们的本质都是给某个函数的 参数（params）而已！


// 内置命令后面附带的所有数据（所有选项和参数）最终都以 参数 的形式传递给了函数，
// 外部命令后面附带的所有数据（所有选项和参数）最终都以 参数 的形式传递给了应用程序。
// （而某个应用程序里面，同样使用了某个函数来接收这个参数……）


// 有了以上认知，我们就不用再区分函数和应用程序了，
// 而且我们就认为：不管是内置命令还是外部命令，它后面附带的数据最终都以参数的形式传递给了函数！
// 实现一个命令的一项重要工作就是解析传递给 函数的参数。


// 命令后面的 选项 和 参数 由 空格 来分割，
// 他们被分割成来多少分，就会作为多少个 参数 传给 函数。
