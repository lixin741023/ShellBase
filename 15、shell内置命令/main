// Shell 命令分为外部命令和内置命令：
// |>
// 内置命令就是 Bash Shell 自带的命令，而不是文件系统中的某个可执行文件。
// |>
// 外部命名是 Linux 系统中的应用程序。
// $PATH 变量包含的目录中几乎聚集了系统中绝大多数的可执行命令，它们都是外部命令。

// 执行外部命令时不但会触发磁盘 I/O，
// 还需要 fork 出一个单独的进程来执行，执行完成后再退出。
// 而执行内建命令相当于调用当前 Shell 进程的一个函数。

// Shell 内置命令有很多，比如常用的 cd、echo、exit、kill、read、printf 等，本章我们将会介绍几个常用的。


